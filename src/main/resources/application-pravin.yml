spring:
  application:
    name: jobs-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/jobportal-app
    username: postgres
    password: pravin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: codepravo@gmail.com   # Gmail address (App password required)
    password: gvey ytio ufyp zxan   # Gmail app password
    protocol: smtp
    test-connection: false
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  output:
    ansi:
      enabled: ALWAYS

logging:
  level:
    "[org.springframework.security]": WARN
